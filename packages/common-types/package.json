{
  "name": "@matrus/common-types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for Matrus applications",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.1.3"
  }
}